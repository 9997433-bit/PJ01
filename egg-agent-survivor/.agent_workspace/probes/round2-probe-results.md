模型 slug: gpt-5.6-sol-xhigh-fast

# Round 2 移动端布局探针结果

## 执行结果

### `node scripts/probe.js`

- 结果：`PASS`
- 汇总：9 passed，0 failed
- 已检查：
  - R2 文件清单完整；
  - 4 个 Node 测试文件可发现；
  - 四边安全区、动态视口高度、compact/portrait HUD、44px 触控目标、
    摇杆排除区共 6 项 CSS 契约；
  - 20 个本地 HTML 引用；
  - 19 个入口脚本引用；
  - 40 个 JavaScript 文件通过 `node --check`。

### `node --test tests/*.test.js`

- 结果：`PASS`
- 汇总：40 tests，38 passed，0 failed，2 todo
- 总耗时：~3600ms
- R2 新增的 5 个移动端布局用例全部通过：
  1. `320×568`、`375×667`、`390×844`、`620×844`、`620×520`、
     `621×844`、`768×1024`、`860×900`、`861×521`、`1024×500`
     的断点边界；
  2. 旋转时 HUD 模式与摇杆保留区同步；
  3. 摇杆底盘避开顶部 HUD、刘海与底部 Home Indicator；
  4. 摇杆限制在左半屏，并忽略右半屏与交互控件；
  5. CSS 安全区、`100dvh`、断点和触控尺寸契约。

## 结论

R2 自动化探针与全量测试均为绿色；移动端 HUD 在竖屏使用顶部双行布局，
短横屏使用顶部单行紧凑布局，下半屏保持为无常驻 HUD 的摇杆控制区。
